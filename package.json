{
  "name": "cavaliereArcEnCiel",
  "version": "1.0.0",
  "description": "L'odyssée de la cavalière arc en ciel",
  "scripts": {
    "postinstall": "node res-generator/copy-resources.js",
    "prepare": "cordova prepare && node res-generator/copy-resources.js",
    "build": "npm run prepare && cordova build"
  },
  "dependencies": {
    "fs-extra": "^10.0.0"
  },
  "cordova": {
    "plugins": {},
    "platforms": [
      "android"
    ]
  },
  "devDependencies": {
    "cordova-android": "^13.0.0",
    "cordova-ios": "^7.1.0"
  }
}